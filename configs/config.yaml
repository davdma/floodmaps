defaults:
  - paths: default
  - analysis:
  - sampling:
  - preprocess:
  - inference: inference_sar
  - tuning:
  - benchmarking:
  - ad:
  - s1_unetpp_cvae_benchmark_saved
  - _self_

# hydra config
hydra:
  # output_subdir: null -> disable hydra completely
  run:
    dir: ${paths.base_dir}/hydra # keeps hydra outputs from cluttering directory