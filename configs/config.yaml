defaults:
  - paths: default
  - inference: # inference_s2
  - pretraining: # default
  - preprocess: # preprocess_manual_experiment
  # - pretrain_s2_unet # s2_unet_infer_v2 # for training
  - s2_unet_ns_exp
  - _self_

# hydra config
hydra:
  # output_subdir: null -> disable hydra completely
  run:
    dir: ${paths.base_dir}/hydra # keeps hydra outputs from cluttering directory